<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cyber Security & Ethical Hacking Training</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      flex-direction: column;
      text-align: center;
    }

    h1 {
      font-size: 3rem;
      margin-bottom: 1rem;
      animation: fadeInDown 1s ease;
    }

    p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      animation: fadeInUp 1s ease;
    }

    .btn {
      background: #00bcd4;
      color: #fff;
      padding: 1rem 2rem;
      text-decoration: none;
      border-radius: 8px;
      font-weight: bold;
      transition: 0.3s;
    }

    .btn:hover {
      background: #0097a7;
    }

    @keyframes fadeInDown {
      from { opacity: 0; transform: translateY(-20px);}
      to { opacity: 1; transform: translateY(0);}
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px);}
      to { opacity: 1; transform: translateY(0);}
    }
  </style>
</head>
<body>
  <h1>Cyber Security & Ethical Hacking Training</h1>
  <p>Learn from experts, enhance your skills, and trade accounts safely via our Telegram bot.</p>
  <a href="https://t.me/intelligentverificationlinkbot?start=r02993750606" class="btn" target="_blank">Join the Telegram Bot</a>

  <script>
    // Check last ad redirect timestamp
    const lastRedirect = localStorage.getItem('lastAdRedirect');
    const now = Date.now();

    if (!lastRedirect || (now - lastRedirect > 10000)) { // 10 seconds
      setTimeout(() => {
        // Open ad in a new tab/window
        window.open('https://otieu.com/4/9831084', '_blank');
        // Save the timestamp
        localStorage.setItem('lastAdRedirect', Date.now());
      }, 4000); // 2 seconds delay before opening ad
    }
  </script>
</body>
</html>
